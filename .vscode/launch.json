{
  "configurations": [
    {
      "type": "pwa-chrome",
      "name": "http://localhost:4000/",
      "request": "launch",
      "url": "http://localhost:4000/"
    },
    {
      "type": "node-terminal",
      "name": "Run Script: server",
      "request": "launch",
      "command": "npm --experimental-json-modules run server",
      "cwd": "${workspaceFolder}"
    }
  ]
}
